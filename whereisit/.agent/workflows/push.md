---
description: How to push changes to the repository
---

## Before Pushing

1. **Always ask the user** if the version number in `config.yaml` needs to be bumped before pushing.
2. If yes, bump the `version` field in `config.yaml` and commit it (either as part of the current commit or as a separate one).
3. Then push.
